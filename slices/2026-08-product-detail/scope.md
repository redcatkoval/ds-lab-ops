# Границы среза

## Граница чтения

- `medusa-src/packages/admin/dashboard/src/routes/products/product-detail/`
- и то, что она импортирует из `dashboard/src/components/` и
  `@medusajs/ui` — списки в `components.md`.

Всё за пределами — отдельный запрос на расширение, не решение по ходу.

## Граница изменения

Внутри `medusa-src/` нельзя изменять ничего, включая саму границу чтения.
Всё, что мы строим, живёт в `ds-ops/` — DEC-003.

## Планируемое исключение

Позже, для эксперимента, появится исключение: файлы с маркером
`.sandbox.` внутри `product-detail/components/`. Механизм ещё не заведён
— ни маркер, ни правило его обработки, ни способ держать такие файлы
вне git-статуса `medusa-src/`. Пока это запись о намерении, а не
действующее разрешение. До того, как механизм заведён, граница
изменения выше действует без исключений.

## Правило на время среза

Работа только внутри этих границ.

**Дата.** 2026-08-28
