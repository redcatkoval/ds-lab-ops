# Контракт: ActionMenu

**Статус:** enforced — два правила проверяются, три выполняются
по построению
**Написан:** 2026-09-01
**Проверил:** Павел, 2026-09-01
**Включён в CI:** 2026-09-01
**Компонент приведён к контракту:** 2026-09-02
**Последняя правка контракта:** 2026-09-02 — см. журнал в конце
**Компонент:** `dashboard/src/components/common/action-menu/`
**Факты:** `ds-ops/slices/2026-08-product-detail/action-menu-facts.md`
**Решения:** приняты Павлом 2026-09-01 в разговоре.

Наблюдения даны со ссылкой на файл и строку. Решения помечены словом
**Решение**. Пути внутри дашборда — от
`medusa-src/packages/admin/dashboard/src/`.

Каждое правило помечено одной из трёх меток:
**[проверяется линтером]**, **[проверяется человеком]**,
**[рекомендация]**. Разница объяснена в начале раздела 4.

## Статусы

| статус | что значит |
|---|---|
| draft | написан агентом из кода, человеком не проверен |
| reviewed | прочитан и подтверждён, но не проверяется |
| enforced | линтер проверяет, CI блокирует нарушения |

Сейчас — **enforced.** Но доля, которую проверяет машина, изменилась
не так, как ожидалось при переводе в этот статус 2026-09-01. Компонент
приведён к контракту 2026-09-02, и три правила из пяти перестали быть
нарушимыми: их выполняет сам компонент, а вызов на них не влияет.

Из девяти правил раздела 4 меткой **[проверяется линтером]** помечены
пять. Их состояние на 2026-09-02, вечер:

| правило | раздел | состояние |
|---|---|---|
| действий два или больше | 4.1 | **проверяется**, `action-menu/min-actions` |
| разрушающие действия последними | 4.3 | **проверяется**, `action-menu/destructive-last` |
| разделитель между всеми действиями | 4.2 | выполняется по построению |
| разрушающее действие помечено красным | 4.3 | выполняется по построению |
| триггер без рамки | 4.5 | выполняется по построению |

**Живых проверок две**, и обе проверяют то, что задаёт вызов, — то есть
то, в чём ещё можно ошибиться. Число действий и их порядок компонент
за автора не исправит.

**Нереализованных из пяти не осталось.** Правило 4.3 о порядке было
последним: признак `destructive` появился в типе (`action-menu.tsx:20`)
2026-09-02, и в тот же день проверка реализована.

**Три выполняются по построению.** Разделитель между всеми действиями
ставит компонент (`action-menu.tsx:86`, `:112`); красный цвет он же
назначает по признаку `destructive` (`:76`, `:98`); проп `variant`
удалён из типа (`:32-34`), поэтому вид триггера из вызова не выбирается.
Ни одно из трёх нельзя нарушить снаружи, и проверять в вызовах нечего.

Три правила, помеченных **[проверяется человеком]**, и одна
**[рекомендация]** статусом enforced не затрагиваются — они и не должны
проверяться линтером. Третье из них добавлено 2026-09-02: правило 4.6
о положении триггера говорит не о вызове, а о его окружении, и линтеру
недоступно.

**Основание для статуса enforced, подтверждено 2026-09-02.**

1. Правила 4.1 и 4.3 реализованы в линтере — `ds-ops/tools/lint/`.
2. Линтер запускается в CI форка на пуш в `ds-lab/**` и на
   пул-реквест с базой `develop` —
   `medusa-ds-lab/.github/workflows/ds-lab-lint.yml`.
3. Прогон зелёный — PR #2, проверка «Контракт ActionMenu», 2026-09-02.
4. Проверка «Контракт ActionMenu» назначена обязательной в ruleset
   `ds-lab` на ветку `develop` форка — то есть нарушение блокирует мерж.
5. 46 существующих нарушений заморожены в
   `ds-ops/tools/lint/baseline.json` и ошибкой не считаются — все 46
   по правилу 4.1.

**Что мешает назвать статус полным.** Не нереализованное правило —
таких больше нет, — а область, до которой проверки не дотягиваются.
Правило 4.1 не может дать ответ по 10 вызовам, правило 4.3 — по 15.
Эти вызовы не «чисто», а «неизвестно», и разница между 10 и 15 сама
по себе говорит о том, чего каждой проверке не хватает — раздел 6,
пункт 3.

---

## 1. Назначение

ActionMenu — меню действий над одной сущностью, свёрнутое под один
триггер. Раскрывается по нажатию, содержит плоский список действий,
каждое из которых либо ведёт по ссылке, либо вызывает обработчик.

Применяется в заголовке секции, в строке таблицы и в карточке — там, где
у сущности есть несколько действий и все они не помещаются в интерфейс
кнопками.

Наблюдение: используется в 104 файлах, 115 вхождений тега.

### Разрушающее действие

**Решение, 2026-09-02.** Разрушающим считается действие, которое **само**
производит необратимое изменение — такое, последствия которого
пользователь не может отменить тем же меню.

Два условия, и оба обязательны:

1. **Необратимость.** Последствие нельзя отменить соседним пунктом того
   же меню. Признак — необратимость для человека за экраном, а не тип
   операции: не важно, удаляет ли действие запись, отзывает доступ или
   двигает деньги.
2. **Непосредственность.** Изменение производит само нажатие пункта.
   Переход к форме, в которой такое изменение можно произвести, —
   не разрушающее действие: нажатие пункта ничего не меняет, а форму
   ещё можно закрыть.

Из определения следует, в частности:

- удаление, отзыв и отмена — разрушающие: нажатие пункта производит
  изменение, и ни одно из них не отменяется соседним пунктом;
- тумблер — не разрушающий, даже если выключает что-то важное:
  тот же пункт меню возвращает прежнее состояние;
- редактирование — не разрушающее: форма открывается заново;
- **переход к форме возврата платежа — не разрушающее**, хотя сам
  возврат необратим. Опасность в форме, а не в переходе.

Практическое следствие второго условия: действие, заданное через `to`,
разрушающим не бывает — ссылка ведёт на экран, а не производит
изменение. Без этого условия по той же логике пришлось бы красить
«Редактировать»: за той формой тоже стоят необратимые изменения.

Наблюдение, 2026-09-02: из 76 действий с признаком все 76 заданы через
`onClick`; действий с признаком и `to` не осталось.

Определение решает случаи, которые прежняя формулировка не решала:
раньше разрушающее опознавалось по иконке `Trash` и метке
`actions.delete`, то есть по внешнему виду. Разбор 182 действий
2026-09-02 нашёл четыре пограничных случая, на которых опознание
по виду расходится с опознанием по определению: отзыв ключа API
(разрушающее, признак стоит), переход к возврату платежа
(не разрушающее по второму условию), переключатель налога
(не разрушающее по первому) и `actions.disable`, который на деле
удаляет — [Q-016](../questions/Q-016-metka-disable-pri-udalenii.md).

Разрушающее действие помечается `destructive: true`. Ставит признак
вызов — компонент опознать разрушающее не может.

## 2. Когда не использовать

**Решение.** При одном действии ActionMenu не применяется. Нужна
иконочная или текстовая кнопка.

Одно действие под меню — это два клика вместо одного.

**Чем заменять — образец.** Вид кнопки задан
[DEC-015](../decisions/DEC-015-obrazec-zameny-menyu-na-knopku.md):
`<Button size="small" variant="secondary">`, с `asChild` и `Link` для
перехода, без иконки. Образец найден обходом 17 шапок секций, а не
придуман. Первое применение — `product-media-section.tsx:89`,
коммит `ae40f47a`.

**Основание, добавлено 2026-09-02.** Это частный случай
[DEC-014](../decisions/DEC-014-osnovnye-knopkami-prochie-v-menyu.md):
основные действия выносятся кнопками, прочие уходят в меню. Если
действие одно, оно по определению основное — элемент существует ради
него, — и потому кнопка, а не меню.

До DEC-014 правило читалось счётным: «меньше двух — нарушение». Теперь
у него есть причина, и она не в числе пунктов.

Наблюдение, перемерено 2026-09-02: сейчас так в 45 вызовах
из 106 разобранных. Ещё 1 вызов передаёт пустой список —
`routes/products/product-create/components/product-create-details-form/components/product-create-details-media-section/product-create-details-media-section.tsx:303`.

## 3. API

**Решение.** Плоский список действий вместо групп.

Раздел описывал целевое состояние, пока компонент ему не соответствовал.
С 2026-09-02 он описывает действительность: тип в коде совпадает
с приведённым здесь.

```ts
type Action = {
  icon: ReactNode
  label: string
  disabled?: boolean
  disabledTooltip?: string | ReactNode
  destructive?: boolean
} & (
  | { to: string; onClick?: never }
  | { onClick: () => void; to?: never }
)

type ActionMenuProps = PropsWithChildren<{
  actions: Action[]
}>
```

Сверено с `action-menu.tsx:9-34` — расхождений нет.

Путь, которым это состояние получено:

| изменение | коммит |
|---|---|
| `destructive?: boolean` добавлен в `Action` | `eb766c31` |
| `groups: ActionGroup[]` → `actions: Action[]`, тип `ActionGroup` удалён | `219a8275` |
| `variant?: "transparent" \| "primary"` удалён | `219a8275` |

**Решение.** `variant` у триггера не используется. Выполнено: проп
удалён, умолчание зашито — `IconButton` с `variant="transparent"`
(`action-menu.tsx:39`).

`children` остаётся без изменений. Наблюдение: передан один раз —
`routes/orders/order-detail/components/order-summary-section/order-summary-section.tsx:212`,
подставляет `Button` вместо `IconButton` по умолчанию.

Имя `actions` для поля выбрано при записи контракта; в разговоре
решение звучало как «плоский список действий».

Поля `icon`, `label`, `disabled`, `disabledTooltip` и взаимоисключающая
пара `to` / `onClick` сохранены как были (`action-menu.tsx:9-30`).

**Следствие для дерева доступности.** Вместе с группами из разметки
исчез элемент `role="group"`. Потеря принята решением
[DEC-012](../decisions/DEC-012-ploskiy-spisok-bez-role-group.md); там же
измерено, что навигация с клавиатуры и объявление пунктов не задеты.

## 4. Правила

Правила двух видов, и оба обязательны.
**[проверяется линтером]** — линтер находит нарушение сам и блокирует
мерж. **[проверяется человеком]** — правило обязательно ровно так же,
но статически не ловится, поэтому его проверяют на ревью.
Отдельно помечена одна **[рекомендация]** — она необязательна.

### 4.1 Число действий

**Решение.** Действий два или больше. Ноль и одно запрещены. **[проверяется линтером]**

**Решение.** До пяти действий. **[рекомендация]**

Это единственная рекомендация в контракте. Превышение не ошибка:
ни линтер, ни ревью его не блокируют. Больше пяти — повод пересмотреть
состав меню.

Правило о двух действиях линтер проверяет только там, где список задан
литералом. Наблюдение: из 115 вхождений список собирается в рантайме
в 10 (перечислены в фактах, раздел «Чего этот способ не видит»).
Эти 10 остаются на ревью.

### 4.2 Разделители

**Решение.** Разделитель ставится между всеми действиями. Группировки
нет. **[выполняется по построению]**

> Метка изменена 2026-09-02. Была **[проверяется линтером]** —
> проверять было нечего, пока разделитель ставился между группами.
> Теперь проверять нечего по обратной причине: нарушить нельзя.

Разделитель — свойство компонента, не вызова. В API поля для него нет.
Компонент ставит его между соседними действиями и только там —
`action-menu.tsx:86` и `:112`, условие `!isLastAction`.

Проверено рендером 2026-09-02 на восьми историях витрины: во всех
разделителей ровно на один меньше, чем пунктов.

### 4.3 Разрушающее действие

**Решение, уточнено 2026-09-02.** Действия с `destructive: true` идут
последними в списке. Между собой — в любом порядке.
**[проверяется линтером]**

> Прежняя формулировка — «действие с `destructive: true` всегда последнее» —
> молча предполагала, что такое действие одно. Это была дыра: меню
> с двумя разрушающими действиями формально нарушало правило, каким бы
> ни был их порядок. Разбор 2026-09-02 такой случай нашёл —
> `api-key-general-section.tsx`, удаление ключа и отзыв ключа в одном
> меню.

Проверяемое условие: в списке нет действия без признака после действия
с признаком. Именно оно, а не «разрушающее последнее»: несколько
разрушающих подряд правилу не противоречат.

Признак читается среди полей верхнего уровня действия — вложенные
объекты не смотрятся. Действия из условного спреда считаются
присутствующими, так же как в правиле 4.1.

Проверено 2026-09-02 на четырёх случаях порядка: «обычное, разр., разр.»
— чисто; «разр., разр., обычное» — нарушение; «разр., обычное, разр.» —
нарушение; «два обычных» — чисто. Отдельно проверено, что правило
не зашито на результат: перестановка действий в копии реального вызова
даёт нарушение.

> Реализовано 2026-09-02 — `action-menu/destructive-last`. Признак
> `destructive` в типе есть (`action-menu.tsx:20`), порядок элементов
> в литеральном списке разбирается тем же способом, что и их число.

**Решение.** Разрушающее действие помечено красным.
**[выполняется по построению]**

> Метка изменена 2026-09-02. Цвет назначает компонент по признаку
> `destructive` — `action-menu.tsx:76` и `:98`, класс
> `text-ui-fg-error [&_svg]:text-ui-fg-error`. Вызов на цвет не влияет
> и отменить его не может.

Токен назван решением
[DEC-011](../decisions/DEC-011-krasnyy-v-menyu-fg-error.md): `fg-error`,
токен переднего плана. Раздел 5 больше не оставляет его неназначенным.

**Оговорка, без которой правило читается неверно.** «По построению»
здесь означает: *если* признак проставлен. Проставить его обязан вызов,
и сейчас он проставлен у 1 действия из примерно 70 — см. раздел 6.

**Решение.** Разрушающее действие вызывает подтверждение.
**[проверяется человеком]**

Статической проверкой не покрывается: наличие подтверждения внутри
обработчика по типу не выражено. См. раздел 7.

### 4.4 Остальные действия

**Решение.** Действие, не помеченное `destructive`, либо открывает
шторку, либо переходит по ссылке. **[проверяется человеком]**

Переход по ссылке проверяется по наличию `to`. Открытие шторки внутри
`onClick` статической проверкой не отличается от любого другого
обработчика.

Наблюдение, перемерено 2026-09-02: из 192 действий в 106 разобранных
вхождениях 102 задают `to`, 90 — `onClick`. Действий без того и другого
нет.

### 4.5 Триггер

**Решение.** Триггер без рамки. **[выполняется по построению]**

> Метка изменена 2026-09-02. Проп `variant` удалён из `ActionMenuProps`
> (`action-menu.tsx:32-34`, коммит `219a8275`). Вид триггера из вызова
> больше не выбирается: умолчание зашито — `IconButton` с
> `variant="transparent"` (`action-menu.tsx:39`).

Правило выполняется не потому, что его соблюдают, а потому, что
нарушить его нечем. Единственный вызов, который передавал
`variant="primary"` — `components/data-table/data-table.tsx` — исправлен
тем же коммитом, и его запись ушла из baseline.

### Что делать с проверкой `action-menu/no-trigger-variant`

**Решение, 2026-09-02: проверку в её нынешнем виде убрать и заменить.**
Не выполнено — правка линтера отдельным шагом.

**Почему убрать.** Проверка ищет проп `variant` в вызовах `<ActionMenu`.
Такого пропа в типе нет, и любой вызов, который его передаст, отвергнет
компилятор — проверено на этой же правке: `tsgo` дал ошибку
`Property 'variant' does not exist`, пока проп не убрали из
`data-table.tsx`. Проверка не находит ничего и найти не может.
Мёртвая проверка хуже отсутствующей: она попадает в строку «Правила:»
каждого прогона и завышает представление о том, что машина стережёт.

**Почему заменить, а не просто удалить.** Само правило не исчезло, оно
переехало из вызовов в компонент. Формулировка выше — «проверяется
отсутствием у компонента props, управляющих видом триггера» — с самого
начала описывала проверку на компоненте; проверка вызовов была
временным заменителем, пока проп существовал. Теперь заменитель мёртв,
а настоящая проверка стала возможна: читать `ActionMenuProps` в
`action-menu.tsx` и падать, если там появился проп вида триггера.

Она стережёт причину, а не следствие. Нынешняя ловит вызов, который
воспользовался вернувшимся пропом, — то есть срабатывает после того,
как правило уже нарушено в компоненте.

**Чего это стоит.** Линтер сейчас умеет одно: разбирать теги
`<ActionMenu` в области сканирования. Чтение объявления типа — другой
разбор и другая область (один файл вместо дерева). Это не переделка
правила, а новый вид правила.

**Отвергнутая альтернатива — оставить как есть.** Ноль работы, но
проверка навсегда останется строкой в отчёте, которая ничего не значит.
Если оставлять, честнее было бы пометить её в отчёте как мёртвую,
а это та же работа, что и замена, только без пользы.

**До замены правило 4.5 не считается проверяемым машиной.** В разделе
«Статусы» оно учтено как выполняемое по построению, а не как
проверяемое.

### 4.6 Положение триггера

**Решение, 2026-09-02.** Триггер меню — последний элемент в ряду
элементов управления. После кнопок основных действий, включая `Create`.
**[проверяется человеком]**

Основание — [DEC-013](../decisions/DEC-013-trigger-posledniy-v-ryadu.md):
меню содержит прочие действия над существующим, кнопка `Create` создаёт
новое. Что именно считается основным действием и потому выносится
кнопкой — [DEC-014](../decisions/DEC-014-osnovnye-knopkami-prochie-v-menyu.md). Крайнее положение отделяет меню от кнопок без дополнительной
формы, поэтому правило 4.5 остаётся в силе и `variant` не возвращается.

**Почему не линтером.** Правило говорит не о вызове, а о его окружении.
Чтобы проверить его машиной, надо разобрать раскладку соседних
элементов: найти родителя вызова, убедиться, что это ряд, перечислить
его детей в порядке следования и понять, какие из них элементы
управления. Ни одного из четырёх шагов линтер сейчас не умеет — он
разбирает тег и его атрибуты, а не дерево вокруг.

Отдельная трудность: «ряд» — не синтаксическая сущность. Это `div`
с определёнными классами раскладки, и опознавать его пришлось бы
по классам, то есть догадкой. Правило, проверяемое догадкой, контракт
не заводит — то же соображение, что в 7.4.

Правило остаётся на ревью.

**Исключение, 2026-09-02.** Если триггер задан вызовом через
`children`, меню замещает кнопку основного действия и остаётся на её
месте.

Признак различим машинно: `children` передан. Такой вызов один из 115 —
`routes/orders/order-detail/components/order-summary-section/order-summary-section.tsx:212`.

Основание — соседний код того же вызова (`:203`): при одном получаемом
возврате рисуется обычная `Button`, при нескольких — меню на её месте.
То есть меню здесь не свалка прочего, а разрешение неоднозначности
одного основного действия: «принять возврат» становится списком, когда
возвратов несколько. По [DEC-014](../decisions/DEC-014-osnovnye-knopkami-prochie-v-menyu.md)
это основное действие, и по DEC-013 в конец ряда оно не уходит.

Перенести такое меню в конец значило бы оторвать его от места, где
стоит его же однокнопочный вариант.

**Границы.** Правило о порядке, не о виде и не о расстояниях. Там, где
ряда нет — в заголовке секции, в строке таблицы, в карточке, — меню
обычно единственный элемент, и правило ни к чему не обязывает.

**Наблюдение: сколько вызовов не соответствуют.** Замер 2026-09-02,
115 вхождений. Признак нарушения — за элементом `ActionMenu` в том же
родителе следует элемент управления.

| вызов | что стоит после |
|---|---|
| `components/data-table/data-table.tsx:429` | `DataTableActions`, `DataTableAction` |
| `components/common/file-preview/file-preview.tsx:47` | `IconButton` — скачать файл |
| `.../product-create-details-media-section.tsx:235` | `IconButton` — `XMark` |
| `.../product-create-details-media-section.tsx:296` | `IconButton` — `XMark` |
| `routes/orders/order-detail/.../order-summary-section.tsx:212` | `Button`, `CopyPaymentLink`, `Button`, `Button` |

**Было пять из 115.** После исключения про `children` осталось четыре,
после правок 2026-09-02 — два, и оба не подлежат перестановке сейчас.

Остальные 110 соответствуют, но почти все — потому, что ряда вокруг нет
вовсе, а не потому, что порядок выбран.

Разбор пяти, 2026-09-02:

| вызов | что решено | состояние |
|---|---|---|
| `data-table.tsx:429` | переставить — канонический случай, из него выросло DEC-013 | **исправлено** |
| `file-preview.tsx:47` | переставить, хотя нарушение недостижимо: ни один из трёх вызовов `FilePreview` не передаёт `actions` и `url` одновременно | **исправлено** |
| `order-summary-section.tsx:212` | триггер задан через `children` | под исключение |
| `product-create-details-media-section.tsx:235` | не трогать, пока открыт [Q-018](../questions/Q-018-krestik-dubliruet-udalenie.md) | **числится условно** |
| `product-create-details-media-section.tsx:296` | не трогать: список действий пуст, вызов подлежит удалению по разделу 2, решения об этом нет | остаётся |

**Про `:235` — нарушение числится условно.** Крестик `XMark` рядом с меню
исключением не признан, значит формально правило 4.6 нарушено
и перестановка напрашивается. Но крестик и пункт «удалить» в меню зовут
один обработчик `onDelete` — это дубль, разбираемый в
[Q-018](../questions/Q-018-krestik-dubliruet-udalenie.md).

Перестановка закрепила бы дубль: она расположила бы два способа сделать
одно и то же в новом порядке вместо того, чтобы убрать один из них.
Поэтому исход зависит от решения по Q-018:

- уберут пункт из меню — меню исчезнет, нарушения не станет;
- уберут крестик — ряда не станет, нарушения не станет;
- признают оба намеренными — тогда перестановка и станет правильной
  правкой.

До ответа вызов остаётся в списке нарушений 4.6 и не правится.
То же соображение — почему `:296` не правится: сначала решение
по разделу 2, потом положение.

Способ замера — обход дерева тегов: от конца элемента `ActionMenu`
до закрытия родителя, имена на том же уровне вложенности. Он
приблизительный: «ряд» опознаётся по наличию соседей, а не по классам
раскладки. Все пять случаев проверены чтением разметки; на ложные
пропуски способ не проверялся.

`data-table.tsx:429` — тот самый вызов из Q-017, и он единственный,
где после меню стоит именно `Create`.

## 5. Токены

**Отступы** — по DEC-007 (`ds-ops/decisions/DEC-007-shkala-devyat-znacheniy.md`):
девять значений 2, 4, 6, 8, 12, 16, 24, 32, 64 px. Произвольные значения
в скобках не используются.

Наблюдение, перемерено 2026-09-02: в компоненте один класс отступа,
`gap-x-2` (`action-menu.tsx:74`, `:96`) — 8 px, шаг 2. В шкале DEC-007
есть.

**Радиусы** — по DEC-008 (`ds-ops/decisions/DEC-008-radiusy-pyat-znacheniy.md`):
пять значений 2, 4, 6, 8, 12 px плюс `full`, только именованными
ступенями.

Наблюдение: в компоненте радиусов нет ни одного.

**Цвет.** Наблюдение, перемерено 2026-09-02: литеральных цветов
в компоненте нет. Цвет задан токенами `text-ui-fg-subtle` (`:74`, `:96`),
`text-ui-fg-disabled` (`:78`, `:100`) и `text-ui-fg-error` (`:76`, `:98`).

Красный цвет разрушающего действия (правило 4.3) — токен `fg-error`,
назначен решением
[DEC-011](../decisions/DEC-011-krasnyy-v-menyu-fg-error.md) 2026-09-02.
Выбран по роли: `fg-error` — токен переднего плана, `button-danger` —
токен заливки. Прежняя формулировка «конкретный токен не назначен»
больше не действует.

Наблюдение к семейству: измерение всех красных токенов системы —
`ds-ops/tokens/red-usage.json`.

## 6. Что осталось

Раздел назывался «Расхождение с текущим состоянием» и перечислял, чем
компонент отличается от контракта. **2026-09-02 расхождений в компоненте
не осталось — закрыты все шесть.** Раздел переименован и переписан:
теперь он о том, что осталось в вызовах.

### В компоненте — закрыто

| что | было | стало | коммит |
|---|---|---|---|
| структура списка | `groups: ActionGroup[]` | `actions: Action[]` (`:33`) | `219a8275` |
| разделитель | между группами | между всеми действиями (`:86`, `:112`) | `ebfeb35a` |
| признак `destructive` | нет в типе | есть (`:20`) | `eb766c31` |
| красный цвет | нет | по признаку (`:76`, `:98`) | `74ecdd09` |
| `variant` | есть | удалён (`:32-34`) | `219a8275` |
| пустая группа | ветка `return null` | групп нет, ветка удалена | `219a8275` |

Сверка типа с разделом 3 расхождений не даёт. Компонент: 119 строк
против 144 до правок.

### В вызовах — осталось

Вхождений 115, действий 192 в 106 разобранных вызовах.
Перемерено 2026-09-02.

**1. Меню на одно действие и меню на ноль — 46 вызовов.**

| что | вхождений | правило |
|---|---:|---|
| одно действие | 44 | 4.1, раздел 2 |
| пустой список | 1 | 4.1, раздел 2 |

Было 46. Одна запись вычеркнута 2026-09-02 —
`product-media-section.tsx`, меню заменено кнопкой по DEC-015. Это
первое за день уменьшение baseline по существу: прежние перезаморозки
переписывали отпечатки, число записей не меняя.

Все 45 заморожены в `ds-ops/tools/lint/baseline.json`, разложены
по 41 файлу. Ошибкой не считаются, код возврата не поднимают,
но и не исчезли: это долг, а не разрешение.

Пустой список — `routes/products/product-create/components/product-create-details-form/components/product-create-details-media-section/product-create-details-media-section.tsx`.
Он не правится вместе с остальным: по разделу 2 меню там не применяется
вовсе, а чем его заменить — вопрос к продукту, не к контракту.
Решения об этом нет.

Эти 46 — единственное, что линтер сейчас находит, и единственное, что
он способен найти: остальные правила либо выполняются по построению,
либо не реализованы.

**2. Признак `destructive` проставлен — 76 действий.**

Расхождение было новым и закрыто в тот же день. До 2026-09-02 признака
не существовало, к вечеру он стоит у 76 действий в 71 файле.

| | действий |
|---|---:|
| `destructive: true` проставлен | **76** |
| из них через `onClick` | 76 |
| из них через `to` | 0 |

Ноль ссылок — следствие второго условия определения из раздела 1:
действие, заданное через `to`, разрушающим не бывает. Один такой случай
был помечен по первой редакции определения и снят по второй — переход
к форме возврата платежа, `order-payment-section.tsx`.

Опись строилась по двум признакам, иконке и метке, и проверялась
поштучно. Из четырёх пограничных случаев признак получили два: отзыв
ключа API и `actions.disable`, который на деле удаляет
([Q-016](../questions/Q-016-metka-disable-pri-udalenii.md)). Не получили
переход к возврату платежа и переключатель налога.

Порядок при этом не нарушился нигде: правило 4.3 на всех 115 вызовах
даёт ноль нарушений.

**Что осталось от этого пункта.** Не сами действия, а невозможность
проверить их машиной: линтер видит проставленный признак, но не видит
непроставленного — 7.4.

**3. Вызовы, по которым проверка не может дать ответ.**

Их два вида, и это не одно и то же «неизвестно».

| правило | вызовов без ответа | чего не хватает |
|---|---:|---|
| 4.1, число действий | 10 | списка: приходит переменной |
| 4.3, порядок | 15 | элементов: список есть, но состав элементов неизвестен |

**Множества вложены, а не пересекаются частично.** Проверено прогоном
2026-09-02: все 10 вызовов правила 4.1 входят в 15 вызовов правила 4.3;
вызовов, неизвестных только правилу 4.1, — ноль. Это ожидаемо: если
списка нет вовсе, то и элементов нет.

**Первые 10 — список приходит переменной.** Не изменилось с 2026-08:
было 10, осталось 10. Шаг к однородной форме (коммит `c05fe3e4`) сделал
их одинаковыми по виду, но не разбираемыми.

**Ещё 5 — список известен, элементы нет.** Правилу 4.1 они не мешают:
оно считает элементы и не смотрит внутрь. Правилу 4.3 нужно знать про
каждый элемент, разрушающий он или нет, а форма элемента ответа не даёт:

| вызов | форма элемента |
|---|---|
| `claim-inbound-item.tsx` | `!showReturnReason && { … }`, список закрыт `.filter(Boolean) as Action[]` |
| `exchange-inbound-item.tsx` | то же |
| `return-item.tsx` | то же |
| `order-edit-item.tsx` | `...[ !isItemRemoved ? { … } : { … } ]` |
| `tax-region-card.tsx` | `...([ !hasParent && { … }, … ])` |

Объект внутри есть, но добраться до его полей, не вычисляя условие,
нельзя. Записать такой вызов в «чисто» было бы враньём: неизвестный
элемент мог бы оказаться разрушающим и стоять перед обычным.

**Что из этого следует.** Разница между 10 и 15 — не недоработка
линтера, а цена формы записи в вызовах. Первые 10 лечатся тем, чтобы
список задавался литералом; вторые 5 — тем, чтобы условие стояло
вокруг списка (`...(cond ? [x] : [])`), а не внутри элемента
(`cond && x`). Обе правки — в вызовах, не в проверке. Решения о том,
делать ли их, нет; см. 7.7.

## 7. Открытые зависимости

**7.1 Формулировка про триггер.** Правило 4.5 записано как «без рамки».
Точная формулировка зависит от контракта на кнопку, которого пока нет.
До него правило 4.5 читать как запрет на выбор вида триггера из вызова,
а не как описание вида.

**Дополнено 2026-09-02.** У правила нашлась вторая незакрытая сторона:
контракт молчал о положении меню в раскладке. Единственный вызов,
передававший `variant="primary"`, оказался единственным местом, где меню
стоит в ряду кнопок, и без рамки там стало хуже —
[Q-017](../questions/Q-017-trigger-v-ryadu-knopok.md). Довод «один
случай из 115, значит ошибка» был доводом по частоте, а не по существу.

**Закрыто в тот же день правилом 4.6** и решением
[DEC-013](../decisions/DEC-013-trigger-posledniy-v-ryadu.md): меню стоит
последним в ряду, и тогда рамка не нужна. Удаление `variant` при этом
осталось верным: оно сняло с второстепенного элемента вес основного.
Несостыковка была в положении, а не в виде — формулировка Q-017
приведена к этому 2026-09-02 по
[DEC-016](../decisions/DEC-016-formulirovki-cherez-nesostykovku.md). Из трёх исходов Q-017 выбран
не первый (вернуть рамку), а близкий ко второму — исправляется
положение, а не вид. Первая сторона 7.1 остаётся открытой: точная
формулировка «без рамки» по-прежнему ждёт контракта на кнопку.

**7.2 Подтверждение у разрушающих действий.** Не проверено поштучно,
есть ли подтверждение у всех 70.

Пофайловый признак (наличие вызова `prompt(` в файле вызова) даёт:
47 из 70 — в файле есть `prompt(`; 22 — обработчик есть, `prompt(` в
файле нет; 1 — не обработчик, а ссылка `to`.

Признак пофайловый, не по действию: файл может вызывать `prompt(` для
одного действия и не вызывать для другого. Поштучная проверка требует
разбора обработчиков и не проведена.

**7.3 Токен красного цвета** для правила 4.3 назначен 2026-09-02 —
`fg-error`, решение
[DEC-011](../decisions/DEC-011-krasnyy-v-menyu-fg-error.md). Зависимость
закрыта.

**7.4 Опознание разрушающего действия машиной.** Простановка признака
закрыта 2026-09-02: помечены 76 действий, вопрос «кто проставит
остальным» снят. Осталась его вторая половина: **линтер не видит
отсутствия признака.** Он проверяет порядок среди тех действий, что
помечены, но не может сказать, что действие помечено не было.

Опознать непомеченное разрушающее можно только догадкой по иконке
и метке — тем способом, которым составлялась опись 2026-09-02. Заводить
по догадке правило контракт не разрешает: это была бы проверка не того,
что записано, а того, что мы предполагаем. Поэтому новое разрушающее
действие без признака пройдёт мимо всех проверок, и поймать его может
только ревью.

**7.5 Замена проверки 4.5.** Решение принято (раздел 4.5), не выполнено.
Требует нового вида правила в линтере — чтения объявления типа, а не
разбора тегов.

**7.6 Расхождение с копией компонента.** В `packages/plugins/draft-order/`
живёт копия `ActionMenu` с 6 вызовами, вне области линтера и вне этого
контракта. После правок 2026-09-02 у копии и оригинала разный API.
См. [Q-015](../questions/Q-015-kopiya-komponenta-v-plagine.md).

**7.7 Форма записи вызовов, мешающая проверке.** 15 вызовов из 115
не получают ответа хотя бы от одного правила — раздел 6, пункт 3.
Лечится не проверкой, а формой записи в вызовах: литеральный список
вместо переменной (10 случаев) и условие вокруг списка вместо условия
внутри элемента (5 случаев). Решения, править ли их, нет. Вопрос
не праздный: 15 из 115 — это 13% вызовов, по которым контракт молчит.

## 8. Журнал правок

| дата | что изменилось в контракте | почему |
|---|---|---|
| 2026-09-01 | контракт написан, переведён в enforced | два правила реализованы в линтере, проверка включена в CI |
| 2026-09-02 | шапка: статус переформулирован — «одно правило проверяется, три выполняются по построению» | компонент приведён к контракту, три правила стали ненарушимыми |
| 2026-09-02 | раздел «Статусы»: таблица пяти правил пересчитана, основание для enforced подтверждено, число замороженных нарушений 47 → 46 | запись `no-trigger-variant` ушла из baseline: нарушать нечем |
| 2026-09-02 | раздел 3: время переписано с будущего на настоящее, добавлена сверка с кодом и таблица коммитов, добавлено следствие про `role="group"` | тип в коде совпал с типом в контракте |
| 2026-09-02 | правило 4.2: метка **[проверяется линтером]** → **[выполняется по построению]** | разделитель ставит компонент между всеми действиями |
| 2026-09-02 | правило 4.3, цвет: метка → **[выполняется по построению]**, назван токен, добавлена оговорка про непроставленный признак | цвет назначает компонент; DEC-011 |
| 2026-09-02 | правило 4.3, порядок: помечено как ставшее выразимым и ждущее реализации | признак `destructive` появился в типе |
| 2026-09-02 | правило 4.4: наблюдение перемерено — 192 действия, 102 `to`, 90 `onClick` | прежние числа относились к прежней форме вызовов |
| 2026-09-02 | правило 4.5: метка → **[выполняется по построению]**, добавлено решение убрать и заменить проверку | проп `variant` удалён, проверка вызовов стала мёртвой |
| 2026-09-02 | раздел 6 переименован из «Расхождение с текущим состоянием» в «Что осталось» и переписан целиком | в компоненте расхождений не осталось; появилось новое расхождение в вызовах |
| 2026-09-02 | разделы 2 и 5: наблюдения перемерены, ссылки на строки компонента обновлены, назван токен `fg-error` | компонент переписан, прежние номера строк указывали не туда |
| 2026-09-02 | раздел 7: 7.3 закрыт, добавлены 7.4, 7.5, 7.6 | токен назначен; появились новые открытые зависимости |
| 2026-09-02 | правило `action-menu/no-trigger-variant` снято из `tools/lint/rules/action-menu.json` | правка раздела 4.5 разорвала якорь: линтер встал с кодом 2 и потребовал привести правило в согласие с контрактом |
| 2026-09-02 | раздел 1: добавлено определение разрушающего действия — необратимость для человека, а не тип операции | опознание по иконке и метке расходилось с опознанием по последствию на трёх случаях |
| 2026-09-02 | раздел 1: определение уточнено вторым условием — непосредственность: разрушает то действие, которое **само** производит изменение | по определению «через последствие» под признак попал переход к форме возврата платежа; по той же логике пришлось бы красить «Редактировать» |

**Определение разрушающего действия уточнялось дважды за один день.**
Оба раза — не от передумывания, а от встречи с случаем, которого
формулировка не покрывала.

| шаг | определение | что его сломало |
|---|---|---|
| было до 2026-09-02 | опознание по виду: иконка `Trash`, метка `actions.delete` | отзыв ключа API — необратим, но иконка `XCircle`, метка своя. По виду не опознаётся |
| первое уточнение | по **последствию**: необратимо для человека за экраном | возврат платежа — последствие необратимо, но пункт меню лишь открывает форму. Под определение попал, а красить его неверно |
| второе уточнение | по последствию **и непосредственности**: действие само производит изменение | пока держится |

Разница между шагами не косметическая. Первое уточнение перенесло
опознание с внешнего вида на смысл. Второе отделило действие от
перехода к действию — и тем поставило границу, которой раньше не было
вовсе: без неё разрушающим оказывается всё, что рано или поздно ведёт
к необратимому, вплоть до «Редактировать».
| 2026-09-02 | правило 4.3, порядок: «действие … последнее» → «действия … идут последними, между собой в любом порядке» | прежняя формулировка предполагала, что разрушающее действие одно; нашёлся случай с двумя |
| 2026-09-02 | правило 4.3, порядок: помечено как реализованное — `action-menu/destructive-last` | проверка написана и проверена четырьмя случаями порядка |
| 2026-09-02 | шапка и раздел «Статусы»: «одно правило проверяется» → «два правила проверяются»; нереализованных не осталось | правило 4.3 реализовано |
| 2026-09-02 | раздел 6, пункт 3: переписан — два вида «неизвестно» вместо одного, 10 и 15 вызовов, множества вложены | правило 4.3 не может дать ответ там, где 4.1 может: списка достаточно, чтобы посчитать, но недостаточно, чтобы узнать состав |
| 2026-09-02 | добавлена открытая зависимость 7.7 — форма записи вызовов, мешающая проверке | 13% вызовов остаются без ответа хотя бы одного правила |
| 2026-09-02 | раздел 6, пункт 2: «признак почти нигде не проставлен» → «проставлен, 76 действий»; добавлено, что ссылок среди них ноль | простановка закрыта в тот же день |
| 2026-09-02 | 7.4 переписана: вопрос «кто проставит остальным» снят, осталось «линтер не видит отсутствия признака» | признак проставлен; проверить его наличие машиной по-прежнему нечем |
| 2026-09-02 | 7.1 дополнена: у правила 4.5 нашлась вторая незакрытая сторона — контракт молчит о положении меню в раскладке | удаление `variant` ухудшило единственный экран, где меню стоит в ряду кнопок (Q-017) |
| 2026-09-02 | добавлено правило 4.6 — триггер последний в ряду элементов управления, **[проверяется человеком]** | DEC-013: положение отделяет меню от кнопок без рамки, поэтому 4.5 остаётся в силе |
| 2026-09-02 | раздел «Статусы»: правил в разделе 4 стало девять, проверяемых человеком — три | добавлено 4.6 |
| 2026-09-02 | 7.1: вторая сторона закрыта правилом 4.6, первая (формулировка «без рамки») остаётся открытой | из трёх исходов Q-017 выбран не «вернуть рамку», а «исправить положение» |
| 2026-09-02 | раздел 2: правило «при одном действии нужна кнопка» получило основание — частный случай DEC-014 | правило читалось счётным; теперь у него есть причина: единственное действие по определению основное |
| 2026-09-02 | правило 4.6: добавлено исключение — меню с триггером из `children` остаётся на месте кнопки | `order-summary-section.tsx:212`: при одном возврате там `Button`, при нескольких меню на её месте |
| 2026-09-02 | правило 4.6: нарушений пересчитано, 5 → 4 → 2; добавлен разбор каждого и состояние | одно попало под исключение, два исправлены перестановкой |
| 2026-09-02 | правило 4.6: `:235` помечен как нарушение **условное** — исход зависит от Q-018 | перестановка закрепила бы дубль вместо того, чтобы его убрать |
| 2026-09-02 | раздел 2: назван образец замены — DEC-015, с первым применением | «нужна кнопка» не говорило, какая именно |
| 2026-09-02 | раздел 6, пункт 1: 46 → 45 записей, 42 → 41 файл | `product-media-section` вычеркнут: меню заменено кнопкой |
| 2026-09-02 | раздел 6, пункт 1: 45 → 21 запись, 41 → 20 файлов | ещё 24 меню с одним действием заменены кнопками по DEC-015 |
| 2026-09-02 | 7.1: формулировка Q-017 переписана по DEC-016 — вопрос о положении, не о рамке | шапка вопроса называла симптом и вела к отвергнутому исходу «вернуть рамку» |

Правки 2026-09-02 внесены после того, как компонент приведён к
контракту: PR #2 в форке, коммиты `eb766c31`, `74ecdd09`, `ebfeb35a`,
`c05fe3e4`, `219a8275`.
