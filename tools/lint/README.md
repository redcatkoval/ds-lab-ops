# ds-ops/tools/lint — линтер контрактов

## Запуск

Из `ds-ops/`:

```
python3 tools/lint/lint.py
```

Зависимостей нет, только Python 3.

Коды возврата: `0` — новых нарушений нет (замороженные и устаревшие
записи baseline код не поднимают); `1` — есть новые нарушения;
`2` — правило и контракт разошлись, проверка не проводилась.

Флаги: `--root <путь>` — переопределить область сканирования,
`--baseline <файл>` — взять другой baseline,
`--freeze` — переписать baseline текущими нарушениями.

## Откуда берутся правила

Линтер читает `rules/*.json`, а не контракт напрямую.

Контракт написан прозой для человека. Линтер, вытаскивающий правило
из прозы, на деле сопоставляет формулировки: достаточно переписать
абзац, и правило молча перестаёт срабатывать. Отдельный файл правил без
связи с контрактом расходится с ним так же молча.

Поэтому правило машиночитаемо, но несёт якорь в контракт: путь, номер
раздела и дословную цитату (`rules/action-menu.json`, поле `anchor`).
Перед проверкой линтер ищет цитату в контракте. Не находит —
останавливается с кодом 2 и сообщает о расхождении. Молчаливого
расхождения не остаётся: либо правило проверено, либо проверка не
проводилась и об этом сказано.

Статус контракта линтер читает из шапки и печатает. Пока он не
`enforced`, вывод содержит строку о том, что CI мерж не блокирует.

## Что проверяется сейчас

Три правила, и они двух видов. Правила вызовов обходят дерево
(`scan.root`) и разбирают теги; правило объявления открывает один файл
и разбирает объявление типа. Вид правила виден по полю: `jsx` —
правило вызовов, `declaration` — правило объявления.

| id | раздел | что читает | нашёл |
|---|---|---|---:|
| `action-menu/min-actions` | 4.1 | вызовы: действий меньше двух | 21 |
| `action-menu/destructive-last` | 4.3 | вызовы: обычное действие после разрушающего | 0 |
| `action-menu/props-closed` | 3 | объявление `ActionMenuProps`, один файл | 0 |

Числа — прогон 2026-09-08 на 90 вхождениях тега. Все 21 заморожены
в baseline.

### Закрытый набор полей (правило раздела 3)

Правило читает `components/common/action-menu/action-menu.tsx`, находит
`type ActionMenuProps` и сверяет состав полей со списком `allowed`.
Нарушение — поле сверх списка, отсутствие поля из списка или другая
форма правой части: ожидается `PropsWithChildren` от одного объектного
литерала, а пересечение (`… & Прочее`) внутрь не разбирается — поле
могло бы прийти оттуда.

Список `allowed` и якорь правила — один и тот же блок контракта,
объявление типа в разделе 3. Поэтому расхождение видно с обеих сторон:
правка объявления в контракте разрывает якорь (код 2), правка типа
в коде даёт нарушение (код 1).

Путь к файлу записан относительно корня области, поэтому `--root`
в CI переопределяет его так же, как дерево вызовов.

Не найден файл или не найдено объявление — код 2, а не «чисто»:
проверять было нечего, и это не то же самое, что «нарушений нет».

### Как считаются действия (правило 4.1)

Контракт описывает плоский список `actions`, код пока передаёт `groups`.
Линтер понимает обе формы: берёт `actions`, если он есть, иначе
суммирует действия по всем группам `groups`.

Действия из условного спреда (`...(условие ? [{…}] : [])`) считаются
присутствующими. На текущем коде выбор ничего не меняет: вызовов с одним
действием 45 при любом из двух прочтений.

Если хоть одна часть списка собирается в рантайме — `groups` или
`actions` переменной, группа переменной, сборка через `.map()`,
пропсы спредом, — вызов уходит в «НЕ ПРОВЕРЕНО», а не в «чисто».
Таких 10 из 115.

## Baseline

`baseline.json` — существующие нарушения, зафиксированные как долг.
Линтер их показывает отдельным списком, ошибкой не считает и код
возврата из-за них не поднимает. Всё сверх baseline — новое, код 1.

### Как запись переживает сдвиг строк

Номер строки в опознании не участвует. Ключ записи — тройка
`rule` + `file` + `fingerprint`, где `fingerprint` это первые 16 hex
от sha256 имени тега и его атрибутов со схлопнутыми пробелами.
Отпечаток не зависит ни от положения вызова в файле, ни от переносов
строк и отступов внутри самого тега.

`line_hint` в записи есть, но при сверке не читается — он только для
человека, чтобы быстрее найти место.

Кратность решается полем `count`: сколько одинаковых вызовов заморожено
в этом файле. Найдено столько же — все заморожены. Найдено больше —
избыток считается новым. Найдено меньше — запись помечена как устаревшая.
Поэтому одинаковые вызовы не схлопываются в один (нарушение не теряется)
и не засчитываются дважды.

### Когда отпечаток меняется

Если атрибуты вызова переписали — отпечаток другой. Тогда прежняя запись
покажется устаревшей, а изменённый вызов — новым нарушением. Это
осознанный размен: переформатирование отпечаток не ломает, а правка
атрибутов и должна проходить через ревью заново.

### Устаревшие записи

Если замороженное нарушение исправили, линтер печатает блок
«BASELINE УСТАРЕЛ» с отпечатком и предложением вычеркнуть запись.
Ошибкой это не считается, код возврата остаётся 0: код стал лучше,
устарел файл долга. Линтер `baseline.json` не правит — вычёркивает
человек.

## Запуск в CI

Линтер гоняется в GitHub Actions форка — `redcatkoval/medusa-ds-lab`,
файл `.github/workflows/ds-lab-lint.yml`. Класть его в форк разрешает
DEC-010: наш файл, с префиксом `ds-lab-`, которого нет в upstream.

Триггеры: пуш в `ds-lab/**` и пул-реквест с базой `develop`.

Правила, контракт и baseline остаются здесь. Workflow забирает из
`ds-lab-ops` только `tools/lint` и `contracts` — около 68 КБ — и зовёт
линтер с флагом `--root packages/admin/dashboard/src`, потому что
`scan.root` в правиле записан для раскладки на диске разработчика.

Ссылка на `main` без пиннинга: правило действует сразу после коммита
сюда, копий правил в форке нет. Цена — коммит в `ds-lab-ops` может
покрасить сборку форка без единого коммита в форке. Поэтому workflow
печатает SHA `ds-lab-ops` в сводку: красная сборка называет версию
правил, на которой упала.

### Коды возврата в CI

| код | результат | что означает |
|---|---|---|
| 0 | зелёный | новых нарушений нет; замороженные не в счёт |
| 1 | красный | новое нарушение |
| 2 | красный | правило разошлось с контрактом, проверка не проводилась |

Код 2 сравнивает два файла, которые оба лежат здесь. Изменением в форке
его вызвать невозможно, и чинить его надо в `ds-lab-ops`. Сообщение
в CI это говорит прямо.

Зависимостей нет: `python3` на `ubuntu-latest` предустановлен, синтаксиса
новее 3.9 в `lint.py` нет, `setup-python` не нужен. `yarn install`
не нужен тем более — линтер не трогает `node_modules`.

Время прогона: сама проверка — 0.12–0.23 с на 1124 файла. Всё остальное
время съедает клон монорепо, поэтому `fetch-depth: 1`.

### Почему сборка может покраснеть на безобидной правке

Отпечаток записи в baseline — хэш содержимого тега (см. «Как запись
переживает сдвиг строк»). Любая правка замороженного `<ActionMenu>`
меняет отпечаток, даже если само нарушение осталось прежним:
переименовали метку, добавили действие, поменяли обработчик.

Тогда линтер видит два события сразу:

- старая запись baseline — «BASELINE УСТАРЕЛ», кода не поднимает;
- изменённый вызов — **новое нарушение**, код 1, сборка красная.

Код при этом не стал хуже. Это осознанный размен: отпечаток не ломается
от переформатирования, но правка атрибутов означает, что вызов изменился
и его стоит посмотреть заново. В CI это выглядит внезапнее, чем при
ручном запуске, — поэтому записано здесь.

Что делать: посмотреть блок «НОВЫЕ НАРУШЕНИЯ». Если вызов по-прежнему
нарушает правило и исправлять его сейчас не планируется — перезаморозить
(`--freeze`) и вычеркнуть устаревшую запись. Кто и когда вычёркивает —
Q-012, пока не решено.

## Границы проверки

Разбор статический, по тексту `.tsx`. Линтер видит атрибуты, записанные
в теге. Пропсы, приходящие спредом (`{...props}`), он засчитать не может
и печатает их отдельным списком «не проверено» — это не «нарушений нет»,
а «неизвестно».

Директория самого компонента (`components/common/action-menu`) из
области исключена для правил вызовов: там объявление, а не вызовы.
Правило раздела 3 читает этот же файл нарочно — оно и есть проверка
объявления.

## Проверки самого линтера

Проведены 2026-09-01, каждая — прогоном.

Правило 4.5 и механика линтера:

1. **Находит ожидаемое.** 1 нарушение,
   `components/data-table/data-table.tsx:433`, `variant="primary"`.
2. **Не зашит на один результат.** С временной подменой искомого пропа
   на `groups` даёт 114 нарушений из 115 вхождений.
3. **Ловит расхождение с контрактом.** С временно изменённой цитатой
   останавливается с кодом 2 и не проверяет ничего. Повторено после
   добавления правила 4.1, на его цитате.

Baseline:

4. **Как есть.** Новых 0, замороженных 1. Код 0.
5. **Новое нарушение.** `variant` добавлен во второй вызов: новых 1,
   замороженных 1, в списке новых только второй вызов. Код 1.
6. **Замороженное исправлено.** `variant` убран из `data-table.tsx`:
   нарушений 0, напечатан блок «BASELINE УСТАРЕЛ». Код 0.
7. **Сдвиг строк.** 40 строк добавлено сверху: вызов найден на 473
   вместо 433, отпечаток тот же, запись осталась замороженной.
8. **Задвоение.** Нарушающий вызов продублирован: тот же отпечаток
   дважды при `count: 1` — одно заморожено, одно новое.

Правило 4.1:

9. **Находит ожидаемое.** С пустым baseline — 46 нарушений:
   45 вызовов с одним действием, 1 с пустым списком. Совпадает с
   разбором в `action-menu-facts.md`.
10. **Границу держит.** «НЕ ПРОВЕРЕНО» — ровно 10 вхождений, те самые,
    что перечислены в фактах, раздел «Чего этот способ не видит».
    В «чисто» они не попадают.
11. **Не зашит на результат.** С временным `min: 3` даёт 82 нарушения
    по правилу 4.1 — добавляются 36 вызовов с двумя действиями;
    с `min: 1` остаётся 1 — только вызов с пустым списком.
    Распределение совпадает с фактами: 0 — 1, 1 — 45, 2 — 36.
12. **Baseline глушит.** После `--freeze`: 47 нарушений, новых 0,
    замороженных 47, «НЕ ПРОВЕРЕНО» по-прежнему 10. Код 0.

Проверки 5–8 меняют исходники, поэтому проведены не в `medusa-src/`,
а на его точной копии в скретчпаде, с запуском `lint.py --root <копия>`.
Точность копии подтверждена прогоном: до мутаций она даёт тот же
результат, что и настоящее дерево. Копия удалена,
`git status --short` в `medusa-src/` пуст.

Правило раздела 3 (`props-closed`), прогоны 2026-09-08:

13. **Как есть — чисто.** Объявлений проверено 1, нарушений 0, код 0.
14. **Ловит вернувшийся `variant`.** `variant?: "transparent" | "primary"`
    добавлен в тип: «появились поля, которых нет в контракте:
    `variant`», код 1.
15. **Ловит проп под именем, которого нет ни в одном перечне догадок.**
    `triggerLook?: "bare" | "boxed"` с JSDoc внутри: то же нарушение,
    код 1. Ради этого случая правило и сделано закрытым набором,
    а не списком имён.
16. **Ловит поле мимо литерала.** `PropsWithChildren<{…}> & { size?: … }`:
    «форма объявления не та, что в разделе 3», код 1.
17. **Ловит пропажу поля.** `actions` переименован в `items`: названы
    оба — лишнее поле и недостающее, код 1.
18. **Не «чисто», когда проверять нечего.** Тип переименован
    в `MenuProps`: код 2, «объявление не найдено», ни одной проверки
    не проведено.
19. **Ловит расхождение с контрактом.** Цитата якоря временно изменена
    в разделе 3: код 2.
20. **Не ломается на поле в несколько строк.** `actions:` с union
    на следующих строках — по-прежнему одно поле `actions`, код 0.
21. **Убрали мутацию — чисто.** Код 0, тот же вывод, что в 13.

Прогоны 14–18, 20, 21 меняют исходники, поэтому проведены на копии
`dashboard/src` в `/tmp` с `--root <копия>`; копия удалена,
`git status --short` в `medusa-src/` пуст. Прогон 19 менял контракт
и восстанавливал его тем же скриптом; равенство файла до и после
проверено.

Подменённые файлы правил после проверок 2, 3 и 11 восстановлены,
`rules/` в исходном состоянии.
